'use client';
import { useState } from 'react';
import { LockKeyhole, UserPlus } from 'lucide-react';
import { saveSession, signIn, signUp } from '@/lib/supabase-rest';
export default function Login(){const [email,setEmail]=useState('admin@valadvogado.com.br'),[password,setPassword]=useState('ValAdmin@2026'),[msg,setMsg]=useState(''),[loading,setLoading]=useState(false);
 const enter=async(e:React.FormEvent)=>{e.preventDefault();setLoading(true);setMsg('');try{const s=await signIn(email,password);saveSession(s);location.href='/admin'}catch(err:any){setMsg(err.message)}finally{setLoading(false)}};
 const create=async()=>{setLoading(true);setMsg('');try{const s=await signUp(email,password);if(s.access_token){saveSession(s);location.href='/admin'}else setMsg('Usuário criado. Confirme o e-mail enviado pelo Supabase e depois entre.')}catch(err:any){setMsg(err.message)}finally{setLoading(false)}};
 return <main className="admin-login"><div className="login-orb one"/><div className="login-orb two"/><form onSubmit={enter}><div className="login-logo">VAL <span>ADMIN</span></div><small>Painel editorial</small><h1>Editar site e revista</h1><p>Entre para atualizar textos, fotos, enquadramentos e gerar a versão de impressão.</p><label>E-mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Senha<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{msg&&<div className="login-message">{msg}</div>}<button disabled={loading}><LockKeyhole/> {loading?'Aguarde…':'Entrar'}</button><button type="button" className="secondary" onClick={create} disabled={loading}><UserPlus/> Criar usuário de teste</button><footer>Usuário previsto: admin@valadvogado.com.br</footer></form></main>}
